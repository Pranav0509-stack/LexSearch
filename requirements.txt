fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pandas>=2.2.0
pyarrow>=16.0.0
s3fs>=2024.3.0
httpx>=0.27.0
aiofiles>=23.2.0
pydantic>=2.6.0
rank_bm25>=0.2.2
pdfplumber>=0.11.0
beautifulsoup4>=4.12.0
tenacity>=8.2.0
lxml>=5.2.0
# Phase 2 — Brief assistant. Optional at runtime (brief_service imports lazily).
google-generativeai>=0.7.0
# Sanhita Vault — DOCX extraction (PDF via pdfplumber above).
python-docx>=1.1.0
# TLS trust on macOS python.org builds (fixes [SSL: CERTIFICATE_VERIFY_FAILED]).
certifi>=2024.2.2
python-multipart>=0.0.9

# In-house dashboard — Phase 3
# Real-time fanout (admin A sees admin B's edits without F5).
python-socketio>=5.11.0
# Postgres driver. The backend speaks SQL through `db_adapter.py`
# which auto-routes to Postgres when DATABASE_URL is set, else falls
# back to the SQLite path. Binary wheel = zero compile.
psycopg[binary]>=3.1.18
# OAuth (Google + GitHub) for the dashboard sign-in. Access codes
# still work as the fallback path.
authlib>=1.3.0
itsdangerous>=2.1.2
