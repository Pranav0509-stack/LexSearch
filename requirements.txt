# Web
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic-settings>=2.2.1

# Legacy LexSearch data layer
pandas>=2.2.0
pyarrow>=16.0.0
s3fs>=2024.3.0
httpx>=0.27.0
aiofiles>=23.2.0

# Telephony + voice AI
plivo>=4.50.0
# sarvamai SDK currently in flux — use direct httpx calls until SDK stabilises.
# silero-vad loaded lazily (ONNX model ~2MB); comment-out if building slim image.
silero-vad>=5.0.0

# Persistence
sqlalchemy>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
redis>=5.0
celery[redis]>=5.4

# Documents
jinja2>=3.1
weasyprint>=62.0
pypdf>=4.2
qrcode>=7.4

# Payments + AWS + SMS
razorpay>=1.4
boto3>=1.34

# Observability
sentry-sdk[fastapi]>=2.0
structlog>=24.1
prometheus-fastapi-instrumentator>=7.0
prometheus-client>=0.20
opentelemetry-api>=1.25
