[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn server:app --host 0.0.0.0 --port $PORT --workers 2 --timeout-keep-alive 30"
restartPolicyType = "on_failure"
numReplicas = 1

[[mounts]]
source = "india_courts_db"
destination = "/data"
